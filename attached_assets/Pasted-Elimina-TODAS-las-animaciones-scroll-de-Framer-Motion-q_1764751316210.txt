Elimina TODAS las animaciones scroll de Framer Motion que no funcionan y prepara el código para implementar una estrategia simple y probada.

## FASE 1: ELIMINAR código no funcional

ELIMINAR de todos los componentes:
- Imports: motion, useInView, useAnimation, useScroll, useTransform
- Todos los useRef, useEffect relacionados con animaciones
- Variables: controls, inView, variants
- Convertir motion.div → div, motion.h1 → h1, motion.p → p, motion.button → button, etc.

MANTENER intacto:
✅ Carousel del hero
✅ Filtros de paquetes funcionando
✅ Formulario contacto y cotización
✅ Accordion FAQs
✅ Todos los botones y modals
✅ Responsive y estilos
✅ Funcionalidad completa del sitio

Archivos a limpiar:
1. HeroSection.tsx
2. PackageGrid.tsx
3. TestimonialsSection.tsx
4. GeneralInfoSection.tsx
5. ContactSection.tsx (CUIDADO: mantener useEffect del form y selectedPackage)
6. Home.tsx (eliminar dispatch scroll event)
7. useRevealAnimation.ts (ELIMINAR archivo completo)

---

## FASE 2: PREPARAR para nueva estrategia (CSS + Intersection Observer)

### Añade clases CSS preparadas para animaciones

En cada componente, añade clases que usaremos después:

**HeroSection.tsx:**
<div className="hero-content animate-on-scroll"> {/* Título y subtítulo */} <button className="hero-cta animate-on-scroll"> {/* Botones */} ```
PackageGrid.tsx:

text
<div className="section-header animate-on-scroll"> {/* Título */}
<div className="packages-grid"> {/* Grid */}
  <div className="package-card animate-on-scroll"> {/* Cada tarjeta */}
TestimonialsSection.tsx:

text
<div className="section-header animate-on-scroll"> {/* Título */}
<div className="testimonials-grid">
  <div className="testimonial-card animate-on-scroll"> {/* Cada testimonio */}
GeneralInfoSection.tsx:

text
<div className="section-header animate-on-scroll"> {/* Título */}
<div className="info-grid">
  <div className="info-card animate-on-scroll"> {/* Cada card */}
ContactSection.tsx:

text
<div className="section-header animate-on-scroll"> {/* Título */}
<div className="contact-form animate-on-scroll"> {/* Formulario */}
<div className="contact-info animate-on-scroll"> {/* Info cards */}
FASE 3: CSS Base para futuras animaciones
Crea un archivo nuevo: client/src/styles/scroll-animations.css

text
/* Animaciones scroll - Desactivadas por defecto */
/* Se activarán con JavaScript cuando estén listas */

.animate-on-scroll {
  /* Por ahora, todo visible y sin transiciones */
  opacity: 1;
  transform: none;
}

/* Cuando implementemos, usaremos estas clases: */
/*
.animate-on-scroll {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.animate-on-scroll.is-visible {
  opacity: 1;
  transform: translateY(0);
}

.package-card.animate-on-scroll {
  transition-delay: calc(var(--index) * 0.1s);
}

.testimonial-card.animate-on-scroll {
  transition-delay: calc(var(--index) * 0.08s);
}
*/
Importa este CSS en client/src/index.css al final:

text
@import './styles/scroll-animations.css';
FASE 4: Hook preparado (sin activar)
Crea archivo: client/src/hooks/useScrollAnimation.ts

text
// Hook para animaciones scroll con Intersection Observer
// Por ahora retorna función vacía
// Cuando estés listo, implementaremos la lógica

export function useScrollAnimation() {
  // TODO: Implementar Intersection Observer aquí
  // Por ahora no hace nada
  return null;
}
RESULTADO INMEDIATO
✅ Sitio 100% funcional sin código roto
✅ Sin animaciones que no funcionan
✅ Sin imports innecesarios de Framer Motion
✅ Bundle más ligero y carga más rápida
✅ Sin errores en consola
✅ Todo el contenido visible desde el inicio

VENTAJAS PARA IMPLEMENTACIÓN FUTURA
✅ Clases CSS ya colocadas en posiciones correctas
✅ Archivo CSS preparado para activar cuando quieras
✅ Hook creado para añadir lógica después
✅ Sin refactorización necesaria más adelante
✅ Compatible con cualquier librería de animaciones (AOS, GSAP, ScrollReveal, etc.)

SIGUIENTE PASO (cuando quieras añadir animaciones)
Tendrás 3 opciones simples y probadas:

OPCIÓN A - Intersection Observer vanilla:

Activar CSS en scroll-animations.css

Implementar lógica en useScrollAnimation.ts

Sin dependencias externas

OPCIÓN B - Librería AOS (Animate On Scroll):

text
npm install aos
Configuración de 5 líneas

Funciona 100% garantizado

Solo añades data-aos="fade-up" a elementos con clase

OPCIÓN C - ScrollReveal:

text
npm install scrollreveal
API simple y potente

Configuración mínima

Funciona con las clases que ya dejamos

Por ahora, el sitio estará limpio, rápido y funcional. Cuando decidas añadir animaciones, todo estará preparado para implementar en 10 minutos sin tocar estructura.

text

***

## 🔥 CHECKLIST PARA VERIFICAR

Después de aplicar estos cambios:

- [ ] No hay imports de Framer Motion en componentes de secciones
- [ ] Todos los motion.X convertidos a elementos HTML normales
- [ ] Clase `animate-on-scroll` añadida en elementos clave
- [ ] Archivo `scroll-animations.css` creado (con animaciones comentadas)
- [ ] Archivo `useScrollAnimation.ts` creado (hook vacío)
- [ ] CSS importado en index.css
- [ ] Formulario de contacto funciona correctamente
- [ ] Filtros de paquetes funcionan
- [ ] Carousel del hero funciona
- [ ] No hay errores en consola
- [ ] Build exitoso sin warnings de Framer Motion

**Resultado:** Sitio limpio + infraestructura lista para animaciones simples cuando quieras. 🚀